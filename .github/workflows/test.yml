name: Test

on:
  push:
    branches: [main, master]
    paths:
      - app/**
      - src/**
      - test/**
      - meta/**
      - "*.nml"
      - CMakeLists.txt
      - Makefile
      - fpm.toml
      - utils/deps
      - utils/dependencies
      - .github/workflows/test.yml
  pull_request:
    paths:
      - app/**
      - src/**
      - test/**
      - meta/**
      - "*.nml"
      - CMakeLists.txt
      - Makefile
      - fpm.toml
      - utils/deps
      - utils/dependencies
      - .github/workflows/test.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Prepare FlexCTM dependencies
        run: bash utils/dependencies

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes gfortran openmpi-bin libopenmpi-dev libnetcdff-dev libnetcdf-mpi-dev netcdf-bin

      - name: Select parallel NetCDF runtime
        shell: bash
        run: |
          netcdf_mpi_lib=$(ldconfig -p | awk '/libnetcdf_mpi\.so/{print $NF; exit}')
          test -n "$netcdf_mpi_lib"
          netcdf_mpi_libdir=/usr/lib/x86_64-linux-gnu/netcdf/mpi/lib
          test -d "$netcdf_mpi_libdir"
          echo "NETCDF_MPI_LIBDIR=$netcdf_mpi_libdir" >> "$GITHUB_ENV"
          echo "LD_PRELOAD=$netcdf_mpi_lib" >> "$GITHUB_ENV"
          echo "Using parallel NetCDF runtime: $netcdf_mpi_lib"

      - name: Set up fpm
        uses: fortran-lang/setup-fpm@v10
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run unit tests
        run: make test COMPILER=gnu FC=mpifort

      - name: Run two-rank integration tests
        shell: bash
        run: |
          test_exe=$(find build/make/debug/real64/tests -type f -path '*/test/integration_netcdf' -perm -111 -print -quit)
          test -n "$test_exe"
          timeout 60s mpiexec -n 2 "$test_exe"

      - name: Run real32 tests
        run: make test COMPILER=gnu FC=mpifort PRECISION=real32
